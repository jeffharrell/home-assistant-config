# Template alarm panel to filter alarm states in UI
template:
  - alarm_control_panel:
    - name: "Alarm"
      state: '{{ states("alarm_control_panel.alarm") }}'
      default_entity_id: alarm_control_panel.alarm_panel_ui
      code_arm_required: false
      code_format: "no_code"
      arm_away:
        - action: alarm_control_panel.alarm_arm_away
          target:
            entity_id: alarm_control_panel.alarm
      arm_home:
        - action: alarm_control_panel.alarm_arm_home
          target:
            entity_id: alarm_control_panel.alarm
      disarm:
        - action: alarm_control_panel.alarm_disarm
          target:
            entity_id: alarm_control_panel.alarm

timer:
  let_dog_out:
    name: "Let Dog Out"
    duration: "00:30:00"
    icon: mdi:dog

automation:
  - alias: "Alarm - Away mode"
    trigger:
      - platform: state
        entity_id: alarm_control_panel.alarm
        to: "armed_away"
    actions:
      # turn off the devices in house
      - action: scene.turn_on
        data:
          entity_id: scene.house_off
      # update camera motion alerts
      - action: input_boolean.turn_on
        data:
          entity_id: input_boolean.camera_motion_enabled
      # notify if doors or garages are open (with built in delays for leaving)
      - delay: "00:00:15"
      - if:
          condition: state
          entity_id: binary_sensor.doors_and_windows
          state: "on"
        then:
          - action: notify.phones
            data:
              message: "Warning! The following are open: {{ expand('binary_sensor.doors_and_windows') | selectattr('state', 'eq', 'on') | map(attribute='name') | join(', ') }}"
      - delay:
          minutes: 10
      - if:
          condition: state
          entity_id: binary_sensor.garage_doors
          state: "on"
        then:
          - action: notify.phones
            data:
              message: "The garage is still open: {{ expand('binary_sensor.garage_doors') | selectattr('state', 'eq', 'on') | map(attribute='name') | join(', ') }}"

  - alias: "Alarm - Home mode"
    trigger:
      - platform: state
        entity_id: alarm_control_panel.alarm
        to: "armed_home"
    actions:
      # turn off the devices in house
      - if:
          condition: state
          entity_id: input_boolean.guest_mode_enabled
          state: "off"
        then:
          action: scene.turn_on
          data:
            entity_id: scene.house_off
        else:
          action: scene.turn_on
          data:
            entity_id: scene.house_off_guest
      # update camera motion alerts
      - action: input_boolean.turn_on
        data:
          entity_id: input_boolean.camera_motion_enabled
      # notify if doors or garages are open
      - action: tts.speak
        target:
          entity_id: tts.google_ai_tts
        data:
          media_player_entity_id: media_player.master_bedroom_speaker
          message: >
            Alarm armed

            {%- if is_state("input_boolean.guest_mode_enabled", "on") -%}
              Guest mode enabled...
            {%- endif -%}

            {%- if is_state("binary_sensor.doors_and_windows_and_garages", "on") -%}
              Warning! The following are open: {{ expand("binary_sensor.doors_and_windows_and_garages") | selectattr("state", "eq", "on") | map(attribute="name") | join(", ") }}
            {%- endif -%}
          options:
            voice: !secret google_tts_voice
          cache: true

  - alias: "Alarm - Disarmed mode"
    trigger:
      - platform: state
        entity_id: alarm_control_panel.alarm
        to: "disarmed"
    actions:
      # notify on status change
      - action: tts.speak
        target:
          entity_id: tts.google_ai_tts
        data:
          media_player_entity_id: media_player.master_bedroom_speaker
          message: "Alarm off"
          options:
            voice: !secret google_tts_voice
          cache: true
      # update camera motion alerts
      - action: input_boolean.turn_off
        data:
          entity_id: input_boolean.camera_motion_enabled
      # turn on the house if morning
      - condition: time
        before: "08:00:00"
      - action: scene.turn_on
        data:
          entity_id: scene.good_morning

  - alias: "Alarm - Alert"
    trigger:
      platform: state
      entity_id: alarm_control_panel.alarm
      to: "triggered"
    actions:
      # light up the house
      - action: scene.turn_on
        data:
          entity_id: scene.house_alert
      # send an alert
      - action: notify.phones
        data:
          title: "Home"
          message: "Alarm triggered!"
          data:
            entity_id: camera.front_door
            url: "/lovelace/cameras"
            push:
              sound:
                name: "default"
                critical: 1
                volume: 1

  - alias: "Alarm - Re-arm after dog"
    trigger:
      - platform: state
        entity_id: timer.let_dog_out
        from: "active"
        to: "idle"
    actions:
      - action: alarm_control_panel.alarm_arm_home
        data:
          entity_id: alarm_control_panel.alarm
      - delay: "00:01:00"
      - action: automation.turn_on
        data:
          entity_id:
            - automation.alarm_disarmed_mode
            - automation.alarm_home_mode
