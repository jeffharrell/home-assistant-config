group:
  default_view:
    name: Inside
    view: yes
    entities:
      - group.lights
      - group.media
      - group.climate
      - group.scenes
  outside:
    view: yes
    entities:
      - group.weather
      - group.landscaping
      - group.security
  tools:
    view: yes
    entities:
      - alert.garage_door_guest
      - alert.garage_door_jeff
      - alert.garage_door_jillian
      - group.settings
      - group.energy
      - group.batteries
      - group.all_automations
